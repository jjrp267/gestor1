{% extends '::base.html.twig' %}

{% block stylesheets %}
   {{ parent() }}
    <link href="{{ asset('bundles/carpmto/css/tabla.css') }}" rel="stylesheet" type="text/css" />    
{% endblock %}

{% block body -%}
    

    <table>
        <caption>lista de Tareas</caption>
        <thead>
            <tr>
                <th>Id</th>
                <th>Tarea</th>
                <th>Descripción</th>
                <th>Fecha tarea</th>
                <th>Cantidad</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tareas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tarea }}</td>
                <td>{{ entity.desctarea }}</td>
                <td>{% if entity.fechatarea %}{{ entity.fechatarea|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tareas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tareas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tareas_new') }}">
                Crear tarea
            </a>
        </li>
    </ul>
    {% endblock %}
